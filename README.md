# Swift 可选类型 Demo

## 简介

展示 Swift 可选类型的各种用法：可选绑定、nil 合并、可选链。

## 启动和使用

```bash
cd swift-optional-demo
swift run
```

## 教程

### 可选类型

Swift 的核心安全特性，用于表示值可能不存在

### 解包方式

- `if let`: 可选绑定
- `??`: nil 合并运算符
- `?.`: 可选链
- `!`: 强制解包（慎用）
- `guard let`: 提前退出
